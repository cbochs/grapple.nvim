describe("tags", function()
    before_each(function()
        require("grapple.tags").reset("none")
    end)

    describe("#tag", function()
        -- it("creates a tag with a file path and cursor location", function() end)

        -- it("creates a tag with the correct file path when a buffer number is given", function() end)
        -- it("creates a tag with the correct file path when a file_path is given", function() end)

        -- it("creates a tag with the correct cursor location when a buffer number is given", function() end)

        -- it("creates a tag with the correct key when a key is given", function() end)
        -- it("appends a tag to the end of the tag list when a key is not given", function() end)

        -- it("raises an error when neither a buffer or file path are given", function() end)
        -- it("raises an error when the buffer is invalid", function() end)
        -- it("raises an error when the file path does not exist", function() end)
    end)

    describe("#untag", function() end)
    describe("#update", function() end)
    describe("#select", function() end)
    describe("#find", function() end)
    describe("#key", function() end)
    describe("#compact", function() end)
    describe("#next", function() end)
    describe("#save", function() end)
end)
